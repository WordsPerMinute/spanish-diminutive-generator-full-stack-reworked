{
  "name": "spanish-diminutive-generator-fullstack",
  "version": "0.0.0",
  "main": "server.js",
  "engines": {
    "node": "24.x"
  },
  "description": "",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "npm install && cd front-end-react && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "debug": "^4.4.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "google-search-results-nodejs": "^2.1.0"
  }
}
