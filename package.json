{
  "name": "spanish-diminutive-generator-fullstack",
  "version": "0.0.0",
  "private": true,
  "main": "app.js",
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd front-end-react && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^0.20.0",
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "google-search-results-nodejs": "^1.3.0",
    "http-proxy-middleware": "^1.0.5"
  }
}

if (process.env.NODE_ENV) 
{ app.use(express.static('client/build'));
  const path = require('path');
  app.get('*', (req, res) => {
    res.sendFile(path.resolve(__dirname, 'client', 'build', 'index.html')); 
  });
}