{
  "name": "spanish-diminutive-generator-fullstack",
  "version": "0.0.0",
  "main": "server.js",
  "engines": {
    "node": "24.x"
  },
  "description": "",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "rm -rf front-end-react && git clone https://github.com/WordsPerMinute/spanish-diminutive-generator.git front-end-react && cd front-end-react && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^0.20.0",
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "google-search-results-nodejs": "^1.3.0"
  }
}
